<% content_for :lnks2 do %>
  | <%= link_to 'К концерту', edit_concert_path(@member.concert) %>
  | <%= link_to 'К списку', concert_members_path(@member.concert) %>
  | <%= link_to 'Новый', :new_concert_member, data: {confirm: 'Несохраненные данные будут утеряны, Да ?'} %>
  | <%= link_to 'Удалить', concert_member_path(@member.concert, @member), method: :delete, data: { confirm: 'Вы уверены что хотите удалить данного участника ? Он хороший.' } %>
  | <%= link_to_function 'Записать', "$('input[name=commit]').first().click()" %>
  | <%= link_to_function 'Отменить', "$('input[name=cancel]').first().click()" %>
  | <%= link_to 'Флаги', :flags %>
  | <%= link_to '0', [@member.concert, @member], title: 'Просмотр' %>
  |
<% end %>

<%= h1andcontent "Редактирование участника #{@member.name}, #{@member.concert.year}" %>

<%= render 'form' %>