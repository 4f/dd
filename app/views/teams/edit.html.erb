<% content_for :lnks2 do %>
  | <%= link_to 'К списку', :teams %>
  | <%= link_to 'Добавить', :new_team %>
  | <%= link_to 'Просмотр', @team %>
  | <%= link_to 'Удалить', @team, method: :delete, data: { confirm: 'Удаляем ?' } %>
  | <%= link_to_function 'Записать', "$('input[name=commit]').first().click()" %>
  | <%= link_to '0', contacts_path, title: 'Просмотр' %>
  |
<% end %>
<%= h1andcontent "#{@team.name}, редактирование" %>

<%= render 'form' %>
