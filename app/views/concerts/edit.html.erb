

<% content_for :lnks2 do %>
  | <%= link_to 'Новый', :new_concert, data: {confirm: 'Несохраненные данные будут утеряны, Да ?'} %>
  | <%= link_to_function 'Записать', "$('input[name=commit]').first().click()" %>
  | <%= link_to_function 'Отменить', "$('input[name=cancel]').first().click()" %>
  | <%= link_to 'Партнеры', concert_partners_path(@concert) %>
  | <%= link_to 'Участники', concert_members_path(@concert) %>
  | <%= link_to 'Альбомы', concert_alboms_path(@concert) %>
  | <%= link_to 'Видео', concert_videos_path(@concert) %>
  | <%= link_to 'Страница участников', edit_concert_members_page_path(@concert) %>
  | <%= link_to '0', :concert, title: 'Просмотр' %>
  | <%= link_to '4', ura_path(@concert), title: 'Просмотр главной с этим концертом' %>
  |
<% end %>

<%= h1andcontent "Редактирование концерта, #{@concert.year}" %>

<%= render 'form' %>
