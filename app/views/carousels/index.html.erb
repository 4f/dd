#encoding: UTF-8
<% content_for :lnks2 do %>
  | <%= link_to 'К концерту', edit_concert_path(@concert) %>
  | <%= link_to_function 'Записать', "$('input[name=commit]').first().click()" %>
  | <%= link_to '0', :root %>
  |
<% end %>

<%= h1andcontent "Конструктор карусели, #{@concert.year}" %>

<%= render 'form' %>